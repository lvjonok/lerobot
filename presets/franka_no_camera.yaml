# Hardware preset: Franka single arm without cameras
#
# For teleoperation testing and low-dim-only recording.
#
# Usage with lerobot-record:
#   python record.py --preset franka_no_camera --teleop spacemouse \
#       --repo_id local/my_dataset --task "Pick and place"

robot:
  type: crisp_fastapi
  server_url: "http://192.168.50.67:8092"
  max_gripper_width: 0.078
  gripper_velocity: 0.15
  gripper_force: 20.0

teleop_defaults:
  spacemouse:
    type: spacemouse
    translation_scale: 0.0000125
    rotation_scale: 0.000025
    teleop_mode: "left_arm_3D_translation_Z_rotation"
    translation_deadzone: 120.0
    rotation_deadzone: 120.0
  haply:
    type: haply
    ws_uri: "ws://localhost:10001"
    translation_scale: 1.0
    rotation_scale: 1.0
    teleop_mode: "left_arm_3D_translation_Z_rotation"

dataset_defaults:
  fps: 30
  episode_time_s: 60
  num_episodes: 10
  video: false
